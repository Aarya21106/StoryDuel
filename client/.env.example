# Server's public URL (Render/Railway/etc). Leave unset for local dev —
# Vite proxies /api and /socket.io to localhost:3001 automatically.
VITE_API_URL=

# Same Google OAuth client ID as server/.env's GOOGLE_CLIENT_ID.
VITE_GOOGLE_CLIENT_ID=
